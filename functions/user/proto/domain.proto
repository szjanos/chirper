syntax = "proto3";

package js.chirp.user.domain;

import "akkaserverless/annotations.proto";

option (akkaserverless.file).value_entity = {
    name: "User"
    entity_type: "user"
    state: "User"
};

message User {
    string user_name = 1;
    string password = 2;
}
